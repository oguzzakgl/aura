const fs = require('fs');
// read the glb file directly or use a simple parsing.
// Actually, since we need to parse glTF binary, it's easier to use a lightweight library if available, but we might not have it.
console.log("We need a GLTF parser");
